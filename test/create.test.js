'use strict'

import t from 'tap'
import fn from '../src/lib/create-event.js'

const test = t.test
const skip = process.env.CI

test('create an event using the opencollective gql api', async (t) => {
  t.same(true, true)
})
